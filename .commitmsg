Fix paragliding activity dot using FPS station data instead of generic wind

The paragliding indicator in the activity bar was showing orange/marginal
because it was reading the selected lake's wind speed (e.g. 3 mph at a
fishing lake) instead of the FPS on-site sensor. Now the paragliding dot
always evaluates against FPS data regardless of which lake tab is selected.
