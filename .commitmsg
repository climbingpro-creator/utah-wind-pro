Fix pressure gradient baseline offset in server predictor

KSLC and KPVU report station pressure (not SLP), creating a ~7.5mb
baseline difference from altitude alone. Subtract this so the weather-
forcing gradient matches the 0-3mb range the scoring functions expect.
