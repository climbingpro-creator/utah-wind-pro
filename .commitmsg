Add historical backfill endpoint for model bootstrapping

New ?action=backfill&days=3 endpoint replays N days of Synoptic
timeseries data through the predict->verify->learn loop to bootstrap
model weights from historical patterns. Increases function timeout
to 60s for the heavy processing.
