Fix Vite dev server failing to load src/main.jsx

The zero-width non-joiner character (U+200C) in the project path
caused Vite's file resolver to fail during development. Fixed by
explicitly setting root: __dirname in vite.config.js so the path
resolves from import.meta.url instead of process.cwd().
